import ch_XX0
import ch_XX1
import ch_XX2
import ch_XX3
import ch_XX4
import ch_XX5
import ch_XX6
import ch_XX7
import ch_XX8
import ch_XX9
import concurrent.futures

executor = concurrent.futures.ThreadPoolExecutor(max_workers=256)
executor.submit(ch_XX0)
executor.submit(ch_XX1)
executor.submit(ch_XX2)
executor.submit(ch_XX3)
executor.submit(ch_XX4)
executor.submit(ch_XX5)
executor.submit(ch_XX6)
executor.submit(ch_XX7)
executor.submit(ch_XX8)
executor.submit(ch_XX9)


# ch_002.py()